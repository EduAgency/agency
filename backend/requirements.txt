# --- Core ---
Django==5.2.6
djangorestframework==3.16.1
django-environ==0.12.0
psycopg[binary]==3.2.9
django-cors-headers==4.7.0

# --- Auth ---
djangorestframework-simplejwt==5.5.1
django-otp==1.6.1                 # admin 2FA (see plan §10)
qrcode==8.2                       # TOTP enrolment QR codes

# --- Async / queue ---
celery==5.5.3
redis==6.4.0
django-celery-beat==2.8.1         # periodic reconciliation jobs (§5.3)
django-celery-results==2.6.0

# --- HTTP ---
requests==2.32.5                  # gateway API calls (Paystack / Flutterwave)

# --- Storage ---
django-storages[s3]==1.14.6
boto3==1.40.24
Pillow==11.3.0                    # school logos / image uploads

# --- Security / ops ---
cryptography==45.0.6              # gateway key encryption at rest (§5.1)
django-ratelimit==4.1.0           # signup throttling (§10)
django-filter==25.1
drf-spectacular==0.28.0           # OpenAPI schema for the Next.js client
gunicorn==23.0.0
whitenoise==6.9.0
sentry-sdk==2.35.0

# --- Dev / test ---
pytest==8.4.1
pytest-django==4.11.1
factory-boy==3.3.3
ruff==0.12.11
